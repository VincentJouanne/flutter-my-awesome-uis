import 'package:flutter/material.dart';

class AwesomeUI extends StatelessWidget {
  final String name;
  const AwesomeUI({Key? key, required this.name}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
